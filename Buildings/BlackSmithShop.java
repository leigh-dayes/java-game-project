package Buildings;

public class BlackSmithShop extends Building {
    public BlackSmithShop(boolean locked) {
        super(locked);
        setLength(2);
        setWidth(2);
        setHitPoints(15);
    }
}
